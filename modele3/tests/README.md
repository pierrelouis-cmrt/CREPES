# Tests — modèle 3

Les tests vérifient le chargement du paquet compact, les calculs radiatifs et
les invariants numériques principaux.

Depuis la racine du dépôt :

```bash
python modele3/tests/tester_modele3.py
```
